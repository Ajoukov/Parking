{
  "name": "parking-app",
  "version": "1.0.0",
  "description": "Full-stack parking app with React frontend and Node.js backend",
  "main": "server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.2",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {},
  "author": "Your Name",
  "license": "MIT"
}
